# Portfolio frontend

The production-facing React application for Shudong Wang's developer portfolio.

From this directory:

```bash
npm install
npm run dev
```

Use `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` before publishing.
